apiVersion: v1
kind: Service
metadata:
  name: {{ template "backend-tappi.fullname" . }}
  labels:
    app: {{ template "backend-tappi.name" . }}
    chart: {{ template "backend-tappi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "backend-tappi.name" . }}
    release: {{ .Release.Name }}
